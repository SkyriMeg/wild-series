<form method="post" action="{{ path('app_season_delete', {'id': season.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette saison ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
	<button class="btn">Supprimer</button>
</form>
