<form method="post" action="{{ path('app_actor_delete', {'id': actor.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet acteur/actrice ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.id) }}">
	<button class="btn">Supprimer</button>
</form>
